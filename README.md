# dotfiles

## Usage

```bash
$ git clone git@github.com:takeyarise/dotfiles.git
$ cd dotfiles
```

**to install**
```bash
$ stow -v <dir>
$ stow -v nvim/
```

**to remove**
```bash
$ stow -vD <dir>
$ stow -vD nvim/
```
